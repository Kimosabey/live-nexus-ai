services:
  # Python AI Worker
  worker:
    build:
      context: ./ai-worker
      dockerfile: Dockerfile
    env_file:
      - .env.local
    environment:
      - LIVEKIT_ROOM=livenexus-demo
    restart: unless-stopped
    ports:
      - "8080:8080" # Optional: for future health endpoint
